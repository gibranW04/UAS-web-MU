@extends('layouts.app')@section('title', $product->name)@section('content')<div class="max-w-7xl mx-auto px-4 py-12">    <div class="grid grid-cols-1 lg:grid-cols-[1.3fr_0.9fr] gap-10">        <div class="space-y-8">            <div class="overflow-hidden rounded-[2rem] bg-slate-950 shadow-[0_40px_120px_rgba(15,23,42,0.35)]">                <div class="relative">                    <img                        id="mainImage"                        src="{{ asset('storage/' . ($product->images->first()->image ?? '')) }}"                        alt="{{ $product->name }}"                        class="w-full h-[520px] object-cover"                    >                    <div class="absolute inset-x-0 bottom-0 bg-gradient-to-t from-slate-950/95 to-transparent px-6 py-8">                        <p class="text-xs uppercase tracking-[0.32em] text-amber-300/80">Product Detail</p>                        <h1 class="mt-4 text-4xl font-black text-white leading-tight">{{ $product->name }}</h1>                        <p class="mt-4 max-w-2xl text-sm leading-relaxed text-slate-300">Nikmati koleksi terbaik dengan desain eksklusif dan kualitas premium yang dirancang untuk penampilan mewah setiap hari.</p>                    </div>                </div>            </div>            <div class="grid grid-cols-3 gap-3">                @foreach ($product->images as $image)                    <button type="button" onclick="document.getElementById('mainImage').src = '{{ asset('storage/'.$image->image) }}'" class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition hover:border-indigo-500 focus:outline-none">                        <img                            src="{{ asset('storage/'.$image->image) }}"                            alt="{{ $product->name }}"                            class="h-24 w-full object-cover"                        >                    </button>                @endforeach            </div>        </div>        <div class="space-y-6">            <div class="rounded-[2rem] border border-slate-200 bg-white p-8 shadow-[0_30px_60px_rgba(15,23,42,0.08)]">                <span class="inline-flex rounded-full bg-amber-100 px-4 py-2 text-xs font-semibold uppercase tracking-[0.24em] text-amber-700">{{ $product->category->name }}</span>                <h2 class="mt-6 text-4xl font-black text-slate-950 leading-tight">{{ $product->name }}</h2>                <div class="mt-4 flex flex-wrap gap-3 text-sm text-slate-500">                    <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-2">{{ $product->variants->sum('stock') > 0 ? 'Ready Stock' : 'Habis' }}</span>                    <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-2">{{ $product->variants->count() }} Variant</span>                </div>                <p class="mt-6 text-slate-600 leading-relaxed">{{ $product->description }}</p>                <div class="mt-8 rounded-[1.5rem] bg-slate-950 p-6 text-white shadow-[0_25px_50px_rgba(15,23,42,0.18)]">                    <div class="flex items-center justify-between gap-4">                        <div>                            <p class="text-sm uppercase tracking-[0.32em] text-slate-400">Harga mulai dari</p>                            <p id="display_price" class="mt-3 text-4xl font-black text-amber-300">Rp {{ number_format($product->variants->min('price')) }}</p>                        </div>                        <div class="rounded-3xl bg-white/10 px-4 py-3 text-xs uppercase tracking-[0.28em] text-slate-200">Premium</div>                    </div>                    <p class="mt-4 text-sm text-slate-400">Pilih variant di bawah untuk melihat harga akhir dan lengkapi pesanan Anda dengan cepat.</p>                </div>                <form action="{{ route('cart.add') }}" method="POST" class="mt-8 space-y-6">                    @csrf                    <input type="hidden" name="product_name" value="{{ $product->name }}">                    <input type="hidden" name="product_image" value="{{ asset('storage/' . ($product->images->first()->image ?? '')) }}">                    <input type="hidden" id="selected_price" name="price" value="{{ $product->variants->min('price') }}">                    <input type="hidden" id="selected_variant_label" name="variant_label" value="">                    <div class="space-y-3">                        @foreach ($product->variants as $variant)                            <label class="group flex flex-col gap-4 rounded-3xl border border-slate-200 bg-slate-50 p-4 transition hover:border-indigo-500 hover:bg-white/90 {{ $variant->stock == 0 ? 'opacity-50 cursor-not-allowed' : '' }}">                                <div class="flex items-start justify-between gap-4">                                    <div class="flex items-start gap-3">                                        <input                                            type="radio"                                            name="variant_id"                                            value="{{ $variant->id }}"                                            data-price="{{ $variant->price }}"                                            data-label="{{ $variant->color ?? 'Standard' }}{{ $variant->size ? ' - Size ' . $variant->size : '' }}"                                            onclick="document.getElementById('selected_price').value = this.getAttribute('data-price'); document.getElementById('selected_variant_label').value = this.getAttribute('data-label'); document.getElementById('display_price').innerText = 'Rp ' + Number(this.getAttribute('data-price')).toLocaleString()"                                            class="mt-1 h-5 w-5 text-indigo-600 focus:ring-indigo-500"                                            {{ $variant->stock == 0 ? 'disabled' : 'required' }}                                        >                                        <div>                                            <p class="text-lg font-semibold text-slate-900">{{ $variant->color ?? 'Standard' }}{{ $variant->size ? ' - Size ' . $variant->size : '' }}</p>                                            <p class="text-sm text-slate-500">Sisa stok {{ $variant->stock }}</p>                                        </div>                                    </div>                                    <div class="text-right">                                        <p class="text-lg font-bold text-slate-950">Rp {{ number_format($variant->price) }}</p>                                        @if ($variant->stock > 0)                                            <span class="inline-flex items-center rounded-full bg-emerald-100 px-3 py-1 text-xs font-semibold text-emerald-700">Tersedia</span>                                        @else                                            <span class="inline-flex items-center rounded-full bg-rose-100 px-3 py-1 text-xs font-semibold text-rose-700">Habis</span>                                        @endif                                    </div>                                </div>                                @if ($variant->stock > 0)                                    <p class="text-sm text-slate-500">Pilih variant ini untuk menyesuaikan harga dan stok.</p>                                @endif                            </label>                        @endforeach                    </div>                    <div class="rounded-[1.5rem] border border-slate-200 bg-slate-50 p-5">                        <div class="flex items-center justify-between text-sm text-slate-500">                            <span>Kuantitas</span>                            <div class="flex items-center rounded-full border border-slate-200 bg-white shadow-sm">                                <button type="button" onclick="this.nextElementSibling.stepDown()" class="px-4 py-2 text-lg font-bold text-slate-600 transition hover:text-indigo-600">−</button>                                <input type="number" name="qty" value="1" min="1" class="w-16 border-none bg-transparent text-center text-lg font-semibold text-slate-900 focus:ring-0">                                <button type="button" onclick="this.previousElementSibling.stepUp()" class="px-4 py-2 text-lg font-bold text-slate-600 transition hover:text-indigo-600">+</button>                            </div>                        </div>                    </div>                    <div class="flex flex-col gap-4 sm:flex-row">                        <button                            type="submit"                            class="flex-1 rounded-3xl bg-amber-400 px-6 py-4 text-lg font-bold text-slate-950 shadow-xl shadow-amber-300/40 transition hover:bg-amber-300 active:scale-[0.99] disabled:cursor-not-allowed disabled:opacity-50"                            {{ $product->variants->sum('stock') == 0 ? 'disabled' : '' }}>                            {{ $product->variants->sum('stock') == 0 ? 'Stok Habis' : 'Tambah ke Keranjang' }}                        </button>                        <a href="{{ route('home') }}" class="inline-flex items-center justify-center rounded-3xl border border-slate-200 px-6 py-4 text-center text-sm font-semibold text-slate-700 transition hover:bg-slate-100">                            Kembali ke Home                        </a>                    </div>                </form>            </div>        </div>    </div></div>@endsection